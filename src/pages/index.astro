---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FeaturedPosts from '../components/FeaturedPosts.astro';
import avatar from '../assets/avatar.png'; 
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import '../styles/global.css';
import { Image } from 'astro:assets';

const posts = await getCollection('blog');

const featuredPosts = (await getCollection('blog')).filter(
  (post) => post.data.properties.Featured?.checkbox === true
);

const slugText = posts.map((post) => (
  post.data.properties.slug.formula?.type === 'string'
    ? post.data.properties.slug.formula.string
    : null
));

const coverUrl = posts.map((post) => (
  post.data.cover?.type === 'file'
    ? post.data.cover.file.url
    : post.data.cover?.type === 'external'
    ? post.data.cover.external.url
    : null
));
---

<!doctype html>
<html class="scroll-smooth" lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zero One Zen</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      :root {
        --primary-teal: #0d9488;
        --deep-teal: #134e4a;
        --warm-cream: #fdfcf9;
        --ritual-gold: #d4af37;
        --spirit-purple: #7c3aed;
        --earth-brown: #8b5a3c;
        --mystic-gradient: linear-gradient(135deg, #0d9488 0%, #7c3aed 50%, #d4af37 100%);
      }

      * {
        cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><text y="20" font-size="20">🌺</text></svg>'), auto;
      }

      a, button, [role="button"], input[type="submit"], input[type="button"], .clickable {
        cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"><text y="20" font-size="20">✨</text></svg>'), pointer !important;
      }

      body {
        font-family: 'Inter', sans-serif;
        background: var(--warm-cream);
        overflow-x: hidden;
      }

      .serif {
        font-family: 'Playfair Display', serif;
      }

      .mono {
        font-family: 'Space Mono', monospace;
      }

      /* Animaciones místicas */
      .float {
        animation: float 6s ease-in-out infinite;
      }
      
      .float-delayed {
        animation: float 6s ease-in-out infinite;
        animation-delay: 2s;
      }

      @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-20px) rotate(5deg); }
      }

      .fade-in-up {
        animation: fadeInUp 1.5s ease-out;
      }

      @keyframes fadeInUp {
        from { 
          opacity: 0; 
          transform: translateY(60px); 
        }
        to { 
          opacity: 1; 
          transform: translateY(0); 
        }
      }

      /* Gradientes místicos */
      .mystic-gradient {
        background: var(--mystic-gradient);
        background-size: 200% 200%;
        animation: gradientShift 8s ease infinite;
      }

      @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
      }

      /* Efectos de hover místicos */
      .mystic-hover {
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        position: relative;
      }

      .mystic-hover::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: var(--mystic-gradient);
        opacity: 0;
        border-radius: inherit;
        transition: opacity 0.4s ease;
        z-index: -1;
      }

      .mystic-hover:hover::before {
        opacity: 0.1;
      }

      .mystic-hover:hover {
        transform: translateY(-8px) scale(1.02);
        box-shadow: 0 20px 40px rgba(13, 148, 136, 0.2), 0 0 20px rgba(212, 175, 55, 0.3);
      }

      /* Símbolos animados */
      .symbol {
        font-size: 2rem;
        opacity: 0.6;
        transition: all 0.3s ease;
      }

      .symbol:hover {
        opacity: 1;
        transform: scale(1.2) rotate(15deg);
        text-shadow: 0 0 20px var(--ritual-gold);
      }

      /* Divider místico */
      .mystic-divider {
        height: 2px;
        background: var(--mystic-gradient);
        margin: 6rem 4rem;
        border-radius: 2px;
        opacity: 0.5;
        position: relative;
      }

      .mystic-divider::before {
        content: '◊';
        position: absolute;
        top: -10px;
        color: var(--ritual-gold);
        font-size: 1.2rem;
        background: var(--warm-cream);
        padding: 0 10px;
        left: 50%;
      }

      /* Header con glassmorphism */
      .glass-header {
        background: rgba(253, 252, 249, 0.85);
        backdrop-filter: blur(20px);
        border: 1px solid rgba(13, 148, 136, 0.1);
      }

      /* Efectos de texto gradiente */
      .gradient-text {
        background: var(--mystic-gradient);
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        background-size: 200% auto;
        animation: textShimmer 3s ease-in-out infinite;
      }

      @keyframes textShimmer {
        0%, 100% { background-position: 0% center; }
        50% { background-position: 200% center; }
      }

      /* Animal spirits floating */
      .spirit-animal {
        position: absolute;
        font-size: 3rem;
        opacity: 0.3;
        animation: spiritFloat 12s ease-in-out infinite;
        pointer-events: none;
      }

      @keyframes spiritFloat {
        0%, 100% { transform: translate(0, 0) rotate(0deg); opacity: 0.3; }
        25% { transform: translate(20px, -30px) rotate(5deg); opacity: 0.5; }
        50% { transform: translate(-10px, -50px) rotate(-3deg); opacity: 0.4; }
        75% { transform: translate(30px, -20px) rotate(8deg); opacity: 0.6; }
      }

      .scroll-to-articles {
        scroll-behavior: smooth;
      }
    </style>
  </head>
  <body class="text-neutral-900 relative scroll-to-articles">

    <main>
      <!-- Hero Section -->
      <section class="relative min-h-screen bg-gradient-to-br from-teal-50 via-purple-50 to-amber-50 px-6 md:px-20 pb-20 pt-32 fade-in-up overflow-hidden">
        <!-- Símbolos místicos en el header -->
        <div class="absolute top-10 left-10 flex space-x-8">
          <span class="symbol float">🌙</span>
          <span class="symbol float-delayed">⚡</span>
          <span class="symbol float">🔮</span>
        </div>

        <div class="max-w-5xl relative z-10">
          <!-- Título principal con efectos místicos -->
          <div class="relative mb-16">
            <h1 class="text-7xl md:text-8xl serif font-bold leading-tight mb-8">
              <span class="gradient-text">Zero One</span>
              <br>
              <span class="text-neutral-800">Zen</span>
            </h1>
          </div>

          <!-- Descripción principal -->
          <div class="max-w-4xl space-y-6">
            <p class="text-xl md:text-2xl p-4 md:leading-10 text-neutral-700 serif font-medium">
              A <em>trans</em> space where <em class="gradient-text">deep curiosity</em>  
              meets <em class="gradient-text">inner transformation</em> <br>Exploring liminal spaces in between...
            </p>
            
            <p class="text-lg text-neutral-600 leading-relaxed">
              
              <ul class="pl-12 text-xl">
                <li class="font-semibold pb-4 text-teal-700">🧬 technology & soul</li>
                <li class="font-semibold pb-4 text-purple-700">binary & infinite <em>⏳</em></li>
                <li class="font-semibold pb-4 text-amber-700">👁️‍🗨️ knowing & becoming</li>
              </ul>
              <!-- <span class="font-semibold text-teal-700">technology and soul</span>, 
              <span class="font-semibold text-purple-700">binary and infinite</span>, 
              <span class="font-semibold text-amber-700">knowing and becoming</span>. -->
            </p>

            <div class="pt-8 mono text-sm md:text-base tracking-wider text-teal-700 mb-6 opacity-80">
              <span class="inline-block">&gt;</span>
              <span class="ml-2">"In the space between 0 and 1, the entire universe unfolds"</span>
            </div>

            <!-- Pilares con iconos -->
            <!-- <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mt-12 pt-8 border-t border-teal-200">
              <div class="text-center space-y-2">
                <div class="text-3xl">👁‍🗨</div>
                <p class="text-sm font-medium text-neutral-600">Deep<br>Curiosity</p>
              </div>
              <div class="text-center space-y-2">
                <div class="text-3xl">🧘‍♀️</div>
                <p class="text-sm font-medium text-neutral-600">Transformative<br>Space</p>
              </div>
              <div class="text-center space-y-2">
                <div class="text-3xl">🧬</div>
                <p class="text-sm font-medium text-neutral-600">Tech ∩<br>Soul</p>
              </div>
              <div class="text-center space-y-2">
                <div class="text-3xl">🐆</div>
                <p class="text-sm font-medium text-neutral-600">Symbols<br>Rituals</p>
              </div>
            </div> -->

          
          </div>

          <!-- Autor con estilo místico -->
          <div class="mt-8 flex items-center space-x-4">
            <!-- <div class="w-12 h-12 rounded-full overflow-hidden">
              <Image 
                src={avatar} 
                alt="Degio" 
                class="w-full h-full object-cover"
                width={48}
                height={48}
              />
            </div> -->
            <div>
              <p class="text-sm font-medium text-neutral-800 pl-4">
                by <a href="/about" class="gradient-text hover:underline font-semibold clickable">Degio ✺</a>
              </p>
              <!-- <p class="text-sm text-neutral-600 italic">Digital explorer · Bit alchemist · Pattern seeker</p> -->
            </div>
          </div>
        </div>

          <!-- Button to scroll to articles -->
            <div class="mt-12 flex">
              <a href="#articles" class="float px-8 py-4 rounded-full mono text-xl text-slate-950 border-2 font-semibold mystic-hover duration-300 clickable">
                ↓↓
              </a>
            </div>

        <!-- Elemento geométrico místico -->
        <div class="absolute right-80 top-1/2 transform -translate-y-1/2 hidden lg:block opacity-20">
          <svg width="400" height="400" viewBox="0 0 200 200" fill="none" xmlns="http://www.w3.org/2000/svg" class="float">
            <defs>
              <linearGradient id="mysticGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" style="stop-color:#0d9488"/>
                <stop offset="50%" style="stop-color:#7c3aed"/>
                <stop offset="100%" style="stop-color:#d4af37"/>
              </linearGradient>
            </defs>
            <path d="M100,20 L180,100 L100,180 L20,100 Z" fill="none" stroke="url(#mysticGrad)" stroke-width="2"/>
            <circle cx="100" cy="100" r="30" fill="none" stroke="url(#mysticGrad)" stroke-width="1"/>
            <path d="M100,70 L130,100 L100,130 L70,100 Z" fill="url(#mysticGrad)" opacity="0.3"/>
          </svg>
        </div>
      </section>

      <!-- Divider místico -->
      <div class="mystic-divider"></div>

      <!-- Posts Section -->
      <section id="articles" class="px-6 py-20 md:px-20 fade-in-up">
        <div class="flex items-center space-x-4 mb-8">
          <h2 class="text-4xl serif font-semibold text-neutral-800">Featured Contemplations</h2>
          <span class="text-2xl">🗝️</span>
        </div>
        
        <p class="text-lg text-neutral-600 mb-16 max-w-2xl">
          Begin your journey with these transmissions. Each post is a gateway to new dimensions of thought.
        </p>

        <!-- Posts Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-8xl">
          {featuredPosts.map((post, index) => (
            <a href={`/blog/${slugText[index]}/`} class="mystic-hover group flex flex-col md:flex-row bg-white/80 backdrop-blur-sm border border-teal-100 rounded-2xl overflow-hidden shadow-lg clickable">
              <div class={`clickable md:w-2/5 h-64 md:h-80 overflow-hidden relative ${index % 2 === 1 ? 'md:order-2' : ''}`}>
                <img
                  src={coverUrl[index] || "https://images.unsplash.com/photo-1534802046520-7f2d2c2e8f5a?w=600&h=400&fit=crop"}
                  alt=""
                  class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                />
                <div class="clickable absolute inset-0 bg-gradient-to-t from-black/30 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
                <div class="absolute top-4 left-4 text-2xl opacity-70 group-hover:opacity-100 transition-opacity">🧬</div>
              </div>
              <div class="clickable md:w-3/5 p-8 md:p-12 flex flex-col justify-center space-y-4">
                <div class="clickable flex items-center space-x-3 text-sm text-teal-600 font-medium">
                  <span class="mono">#transdisciplinary</span>
                  <span>•</span>
                  <span class="mono">#consciousness</span>
                </div>
                <h3 class="clickable text-2xl md:text-3xl serif font-semibold text-neutral-800 group-hover:text-teal-700 transition-colors">
                  {post.data.properties.Name?.title?.[0]?.plain_text || "Untitled Article"}
                </h3>
                <p class="clickable text-neutral-600 leading-relaxed">
                  {post.data.properties.Description?.rich_text?.[0]?.plain_text || "A deep exploration into the intersection of technology and consciousness."}
                </p>
                <div class="clickable flex items-center space-x-2 text-sm text-neutral-500">
                  <span>5 min contemplation</span>
                  <span>•</span>
                  <span class="clickable flex items-center space-x-1">
                    <span>⭐</span>
                    <span>Essential reading</span>
                  </span>
                </div>
              </div>
            </a>
          ))}
        </div>
      </section>

      <!-- Divider místico -->
      <div class="mystic-divider"></div>

      <!-- Newsletter Section -->
      <section class="px-6 md:px-20 py-16">
        <div class="max-w-3xl mx-auto text-center space-y-8">
          <div class="space-y-4">
            <h3 class="text-3xl serif font-semibold gradient-text">Join the Transmission</h3>
            <p class="text-lg text-neutral-600">
              Receive new explorations directly in your digital dimension. 
              No spam, only deep connections.
            </p>
          </div>
          
          <div class="mystic-hover bg-white/80 backdrop-blur-sm border border-teal-200 rounded-2xl p-8">
            <div class="flex flex-col md:flex-row gap-4 items-center justify-center">
              <input 
                type="email" 
                placeholder="your.email@universe.com"
                class="flex-1 max-w-md px-6 py-3 border border-teal-200 rounded-full text-center focus:outline-none focus:ring-2 focus:ring-teal-500 focus:border-transparent clickable"
              />
              <button class="mystic-gradient text-white px-8 py-3 rounded-full font-semibold hover:shadow-lg transition-all duration-300 clickable">
                Connect 🌺
              </button>
            </div>
            <p class="text-sm text-neutral-500 mt-4">
              ~1 transmission per lunar cycle · You can always disconnect
            </p>
          </div>
        </div>
      </section>

      <!-- Footer místico -->
      <footer class="text-center py-16 space-y-6">
        <div class="flex justify-center space-x-8 text-2xl">
          <span class="symbol">🌙</span>
          <span class="symbol">◊</span>
          <span class="symbol">∞</span>
          <span class="symbol">🔮</span>
          <span class="symbol">⚡</span>
        </div>
        
        <div class="space-y-2">
          <p class="text-neutral-600 mono text-sm">
            © 2025 Zero One Zen — Channeled by Degio ✺
          </p>
          <p class="text-neutral-500 text-xs">
            All rights reserved in this dimension and parallel ones
          </p>
        </div>
        
        <div class="flex justify-center space-x-6 text-sm text-neutral-600">
          <a href="/about" class="hover:text-teal-600 transition-colors clickable">About the Mission</a>
          <span>•</span>
          <a href="/archive" class="hover:text-teal-600 transition-colors clickable">Full Archive</a>
          <span>•</span>
          <a href="/contact" class="hover:text-teal-600 transition-colors clickable">Quantum Contact</a>
        </div>
      </footer>
    </main>
  </body>
</html>